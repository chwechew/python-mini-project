# BMI 

A simple Python program to calculate Body Mass Index (BMI) based on user’s height (cm) and weight (kg).

Program ini akan mengkategorikan BMI ke beberapa kondisi (underweight, normal, overweight, dll) dengan output message sederhana.
